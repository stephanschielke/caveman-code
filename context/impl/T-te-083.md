Build site: context/plans/build-site-token-efficiency.md
# T-te-083: Per-turn cost cap mid-stream cancellation
**Status:** done
`packages/agent/src/cost/caps.ts` — `CostCapTracker.recordIncremental()` returns `true` to signal caller to cancel the in-flight LLM stream. Emits `cost_cap_turn` event with turn index, accumulated dollars, cap value.
Test: "fires cost_cap_turn when per-turn cap exceeded", "no cap means call streams to completion".
Acceptance R2 AC-1, AC-2, AC-4 met.
