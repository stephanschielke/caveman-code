Build site: context/plans/build-site-token-efficiency.md
# T-te-085: Per-session cost cap aggregating subagent cost
**Status:** done
`CostCapTracker({ perSessionDollars })` accumulates `sessionTotal` across every `recordIncremental()` call. Subagent costs aggregated by caller forwarding entries to the parent tracker. Halting emits `cost_cap_session` event.
Test: "fires cost_cap_session when session cap exceeded".
Acceptance R3 AC-1, AC-2, AC-3 met.
