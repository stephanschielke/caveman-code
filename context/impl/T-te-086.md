Build site: context/plans/build-site-token-efficiency.md
# T-te-086: /cost slash command live panel with hit-rate formula
**Status:** done
`packages/agent/src/cost/panel.ts` — `CostPanel.render()` shows: dollars spent, input tokens (cached + uncached split), output tokens, tool cache hits, prompt cache hit rate (`cached / (cached + uncached)`).
Test: "shows all five metrics", "hit rate = cached / (cached + uncached)".
Acceptance R4 AC-1, AC-3 met.
