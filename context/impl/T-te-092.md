Build site: context/plans/build-site-token-efficiency.md
# T-te-092: Surface provenance in trace viewer + test-fail on unknown source
**Status:** done
`ProvenanceRegistry.stamp()` throws `provenance: unknown source message id ...` when a sourceMessageId was not previously `register()`-ed. Trace viewer renders provenance via the generic `default` row formatter when a `provenance` event appears.
Test: "throws on unknown source message id (test-visible failure)".
Acceptance R8 AC-2, AC-3 met.
