Build site: context/plans/build-site-token-efficiency.md
# T-te-090: `cave replay <rollout>` deterministic re-execution with --apply gate
**Status:** done
`packages/agent/src/cost/replay.ts` — `replay(events, { dryRun, onLlmCall, onToolCall })`. Sorted by `seq`; LLM calls always replayed into `onLlmCall`; tool calls dispatched only when `dryRun === false`.
Tests: reproduces LLM sequence, `--apply` gating (planned vs executed count).
Acceptance R7 AC-1, AC-2, AC-3 met.
