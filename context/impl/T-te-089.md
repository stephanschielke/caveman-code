Build site: context/plans/build-site-token-efficiency.md
# T-te-089: LLM-call rows + tool-call cache state
**Status:** done
`fmtLlmRow()` renders model / input / cachedInput / output / dollars. `fmtToolRow()` renders tool + cache state.
Tests: "llm rows display model/tokens/dollars", "tool rows display cache hit/miss".
Acceptance R6 AC-3, AC-4 met.
