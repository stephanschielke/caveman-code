Build site: context/plans/build-site-token-efficiency.md
# T-te-088: `cave trace <session>` terminal viewer with filter-by-type
**Status:** done
`packages/agent/src/cost/viewer.ts` — `renderTrace(events, { filter })` sorts by `ts, seq` and renders each row with a type-specific formatter. `--filter <type>` matches on exact type or prefix (`llm_`, `tool_`).
Tests: ordering, filter.
Acceptance R6 AC-1, AC-2 met.
